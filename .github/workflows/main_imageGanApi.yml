# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy container app to Azure Web App - imageGanApi

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: 'ubuntu-latest'

    steps:
    - uses: actions/checkout@v2

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Log in to registry
      uses: docker/login-action@v2
      with:
        registry: https://imageganapi.azurecr.io/
        username: ${{ secrets.AzureAppService_ContainerUsername_141d3c6781a442d18b27e7ca42aac6ca }}
        password: ${{ secrets.AzureAppService_ContainerPassword_032fdb926dbb42ee9233131fd25e5ac6 }}

    - name: Build and push container image to registry
      uses: docker/build-push-action@v3
      with:
        push: true
        tags: imageganapi.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_141d3c6781a442d18b27e7ca42aac6ca }}/image-gan-api-app:${{ github.sha }}
        file: ./Dockerfile

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

    steps:
    - name: Deploy to Azure Web App
      id: deploy-to-webapp
      uses: azure/webapps-deploy@v2
      with:
        app-name: 'imageGanApi'
        slot-name: 'production'
        publish-profile: ${{ secrets.AzureAppService_PublishProfile_74185037e918457888c4c2a1e49f9465 }}
        images: 'imageganapi.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_141d3c6781a442d18b27e7ca42aac6ca }}/image-gan-api-app:${{ github.sha }}'